<nav class="nav" id="mainNav">
  <a href="/listing" class="logo">Rent<span>Pe</span></a>
  <div class="nav-center">
    <a href="/listing">Home</a>
    <a href="#listings">Listings</a>
    <% if(currUser) {%>
    <a href="/profile/<%=currUser._id%>">Profile</a>
    <%}%>
  </div>
  <div class="nav-right">
    <% if(currUser) {%>
    <a href="/listing/new" class="btn-create nav-buttoms"
      >+ Create New Listing</a
    >
    <% }else{ %>
    <a href="/login" class="btn-login nav-buttoms">Login</a>
    <% } %> <%if(currUser){%>
    <a href="/logout" class="btn-login nav-buttoms">Logout</a>
    <%}%>
  </div>
  <button class="hamburger" id="hamburger" aria-label="Toggle menu">
    <span></span><span></span><span></span>
  </button>
</nav>

<div class="mobile-menu" id="mobileMenu">
  <a href="/listing" onclick="closeMob()">Home</a>
  <a href="#listings" onclick="closeMob()">Listings</a>
  <% if(currUser) {%>
  <a href="/listing/mylisting" onclick="closeMob()">My Listing</a>
  <%}%> <% if(currUser) {%>
  <a href="/listing/new" class="mob-create" onclick="closeMob()"
    >+ Create New Listing</a
  >
  <a href="/logout" class="mob-login" onclick="closeMob()">Logout</a>
  <% }else{ %>
  <a href="/login" class="mob-login" onclick="closeMob()">Login</a>
  <% } %>
</div>